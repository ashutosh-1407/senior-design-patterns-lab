[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dungeon-arena"
version = "0.1.0"
description = "A guided SOLID principles and design patterns learning project"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["dungeon_arena*"]
